# Прогоны калибровки People Graph (nexus-calibration)

Каждый прогон — папка `<run_id>/` (short-01, long-01…) с `questions.md`, `review.md`, `correction-prompt.md`, `people-graph.puml`.
Создаётся командой `/radar-calibrate`. Историю итераций не чистим — видна сходимость к N/N.
