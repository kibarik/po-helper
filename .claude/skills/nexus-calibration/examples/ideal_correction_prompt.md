# Эталон: разбор + correction-Prompt

Продолжение `ideal_question_set.md`: прогон `short-01` дал `9/10`, расхождение #7. Ниже — что выдаёт `/radar-review short-01`.

---

## review.md

### Расхождение #7 — дата выката платёжек

- **Вопрос:** «Заказчик: "когда дата выката платёжек?" — к кому?»
- **Ответ LLM (выборка):** `team-petrov-ivan` · почему: `petrov.contact_for "релизы"`, `petrov.influence_zones "релизы"`
- **Правда PO:** дату выката платёжек называет Сидорова; у Петрова инфра-релизы, не платёжные
- **Почему расходится:** зона Петрова `influence_zones: "релизы"` слишком широкая — текстово покрывает и платёжные релизы, которыми он не владеет. У `team-sidorova-anna` платёжная экспертиза есть в `influence_zones`, но нет в `contact_for`/`expertise_topics` в формулировке про *дату выката* → LLM выбрал ближайшего по слову «релиз».
- **Что подправить:**
  - `team-petrov-ivan`: сузить `influence_zones` "релизы" → "инфра-релизы, деплой"; убрать общий "релизы" из `contact_for`
  - `team-sidorova-anna`: добавить `contact_for: "дата выката платёжек"`, `expertise_topics: "график релизов платёжного модуля"`
- **Слой:** Expertise (дизамбигуация)

### Уточнить у PO
_(пусто — правда PO однозначна, догадок нет)_

---

## correction-prompt.md

```markdown
# Correction-Prompt (калибровка short-01, итерация 1)

> Скопируй блок ниже и отправь в чат. После применения — запусти /radar-calibrate short short-02.

---

Откалибруй team-нексус (People Graph) по итогам калибровки short-01. Внеси точечные правки в person-узлы `GROUND/NEXUS/team/`. Меняй только перечисленные поля, остальное не трогай. После правок подними `confidence` затронутых узлов и обнови `updated` + `sources` (добавь `"calibration:short-01"`).

## Узел team-petrov-ivan
- `influence_zones`: "релизы" → "инфра-релизы, деплой"
- `contact_for`: убрать общий "релизы" (оставить инфра-деплойные формулировки)
- причина: платёжные релизы ведёт не он; широкая зона уводила роутинг платёжных вопросов на него

## Узел team-sidorova-anna
- `contact_for`: + "дата выката платёжек"
- `expertise_topics`: + "график релизов платёжного модуля"
- причина: реальный владелец даты выката платёжек, не был выражен в графе в этих терминах

## После правок
Покажи diff обоих узлов и подтверди словами новое понимание: «дата выката платёжек → Сидорова; инфра-релизы и деплой → Петров». Это понимание должно теперь воспроизводиться при роутинге.
```

---

## Что дальше (цикл)

1. PO отправляет блок correction-Prompt в чат → LLM правит `team-petrov-ivan` и `team-sidorova-anna`, показывает diff.
2. PO запускает `/radar-calibrate short short-02`. Часть вопросов нацелена на платёжки/релизы (проверка правки), часть — свежие.
3. Если `short-02` даёт `10/10` → 🟢 КАЛИБРОВКА ПРОЙДЕНА за 2 итерации. Иначе — следующий разбор.

> Правка адресная и минимальная: тронуты ровно два поля у двух узлов, вызвавших `❌`. Нексус не переписан «чтобы тест прошёл» — исправлено расхождение с реальностью, и след правки остался в `sources`.
