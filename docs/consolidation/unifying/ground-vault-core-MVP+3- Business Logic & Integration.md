В рамках инкремента `ground-vault-core:MVP+3` необходимо реализовать высокоуровневую бизнес-логику и обеспечить внешнюю интеграцию системы. Решение направлено на систематизацию управления ожиданиями и обязательствами через специализированные реестры, внедрение иерархической ролевой модели с акцентом на топ-менеджмент, устранение разрывов контекста между различными инструментами (JIRA, Confluence, GROUND, чаты) и финальную калибровку точности через строгий план/факт анализ.

**Требования (агрегированы):**
*   — from #103: Реализовать калибровочный контур **Plan/Fact Radar** для сверки кластерного и квартального план/факт как golden-test po-helper (гейт `N/N`, ноль ошибок), обеспечив обязательное объяснение каждого расхождения (источник, дата синка, узел Нексуса, якорь).
*   — from #109: Внедрить ролевой приоритет в Summary, используя данные из People Graph (ранг, reports_to/org-chart, без хардкода), с созданием отдельного трека для C-Level, подсветкой риска рассинхрона ожиданий PO ↔ C-Level и смещением точности в сторону реплик топ-менеджмента.
*   — from #128: Создать реестр ожиданий и обязательств PO (новая сущность/Нексус, например NEXUS/expectations) для хранения точек контроля (от кого/когда/что/влияние на цель/цена пропуска) с интеграцией в ежедневный отчёт (/daily-review) для напоминания ключевых ожиданий и анализа последствий просрочек.
*   — from #134: Разработать механизм синхронизации артефактов между системами (JIRA, Confluence, GROUND, чаты) для выравнивания контекстов стейкхолдеров на 5–10 командах, включая реестр «один смысл ↔ его представления в разных системах» и периодическую сверку для выявления устаревших данных.